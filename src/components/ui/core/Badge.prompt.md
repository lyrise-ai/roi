Uppercase status pill for state — "In review", "Deployed", "Step 1".

```jsx
<Badge tone="grow">Deployed</Badge>
```

Tones are named after the secondary palette (`grow`, `power`, `sunny`, `trust`). Use `Tag` for user-authored or taxonomy labels instead.
