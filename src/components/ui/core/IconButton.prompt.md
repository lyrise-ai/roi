Circular icon-only button; always pass `label` for accessibility.

```jsx
<IconButton label="Close" variant="ghost">
  <Icon name="x" />
</IconButton>
```

Variants `ghost | outline | solid`; sizes 32/40/48px. Pair with `Icon` at 16–20px.
