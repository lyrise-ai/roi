Transient confirmation, bottom-right stacked.

```jsx
<Toast
  tone="success"
  title="Process map saved"
  message="Shared with 3 stakeholders."
  onDismiss={close}
/>
```

Tones map to `--grow`, `--power`, `--lyrise-purple`.
